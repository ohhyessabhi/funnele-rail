/**
 * Hours (time logging) is spec'd for v2 — the `time_logs` schema exists but the
 * logging UI is deferred. This is the intentional v1 stub.
 */
export function Hours() {
  return (
    <div className="empty">
      <strong>Time log</strong>
      <div className="hint">Coming in the next iteration (v2)</div>
    </div>
  );
}
